# CASCADE — Cartographic specification (frozen)

Salinan dari repo aplikasi. Mode = hue. Status = pattern / casing.

## Hierarchy

BASEMAP → ROAD CONTEXT → EXISTING TRANSPORT → MASTERPLAN (acuan) → CASCADE (usulan) → ANALYSIS → EVIDENCE → SELECTION

## Transport hues

| Mode | Hex |
|---|---|
| KRL | `#1F4E79` |
| MRT | `#E6B800` |
| LRT | `#0D9488` |
| TransJakarta | `#DC2626` |

Existing solid; masterplan dash `[8,4]`; CASCADE casing + class encoding; survey square; bottleneck diamond.
